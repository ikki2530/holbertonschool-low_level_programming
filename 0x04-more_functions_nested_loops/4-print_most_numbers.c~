#include "holberton.h"
/**
 * mul - Multiplies two integers
 * @a: val to multiply
 * @b: val to multiply
 * Return: return the result of multiplication
 */
void print_most_numbers(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
		if (i != '2' && i != '4'){}
	}
}
