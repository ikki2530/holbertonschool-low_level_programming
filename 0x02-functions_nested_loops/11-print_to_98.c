#include "holberton.h"
/**
 * times_table - prints the last digit of n
 *
 * Return: return las digit of n
 */
void print_to_98(int n)
{
	int i;
	char fd;
	char md;
	char ld;

	if (n < 98)
	{
		if
	}
	else if (n == 98)
	{
		_putchar(ld + '0');
	}
}
